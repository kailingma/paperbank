services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: paperbank
      POSTGRES_PASSWORD: paperbank
      POSTGRES_DB: paperbank
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
